<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.jacknic.view">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MaterialComponents.DayNight.DarkActionBar">
        <activity
            android:name="StarterActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".camera.CameraActivity"
            android:label="Camera 基础用法" />
        <activity
            android:name=".rotate3d.Rotate3dActivity"
            android:label="Animation 3D 旋转" />
        <activity
            android:name=".bevel.BevelActivity"
            android:label="圆角斜边卡片布局" />
        <activity
            android:name=".xfermode.XfermodeActivity"
            android:label="xfermode 绘制模式视图" />
    </application>

</manifest>